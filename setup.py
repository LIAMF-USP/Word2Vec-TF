from setuptools import setup, find_packages

setup(
    name='word2vec-tf',
    version='0.1',
    description='Comparison of some word2vec models',
    packages=find_packages(),
    test_suite="tests",
)
